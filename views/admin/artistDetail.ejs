<%- include('../partials/header') %>

<h1><%= artist.name %></h1>
<table class="table">
  <tr>
    <th>Artwork</th>
    <th>Interested</th>
    <th>Notes</th>
  </tr>
  <% visits.forEach(v => { %>
  <tr>
    <td><%= v.artwork.name %></td>
    <td>
      <% if (v.interested) { %>
      ⭐
      <% } %>
    </td>
    <td><%= v.notes %></td>
  </tr>
  <% }) %>
</table>

<%- include('../partials/footer') %>