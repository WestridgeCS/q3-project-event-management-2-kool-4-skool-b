<%- include('../partials/header') %>

<h1>Student Engagement</h1>
<table class="table">
  <tr>
    <th>Student</th>
    <th>Notes Taken</th>
    <th>Interested Colleges</th>
  </tr>

  <% data.forEach(row => { %>
  <tr>
    <td>
      <a href="/admin/artists/<%= row.artist._id %>">
      <%= row.artist.name %>
      </a>
    </td>
    <td><%= row.visitCount %></td>
    <td>
      <% row.interested.forEach(v => { %>
      <span class="tag">
      <%= v.artwork.name %>
      </span>
      <% }) %>
    </td>
  </tr>
  <% }) %>
</table>

<%- include('../partials/footer') %>