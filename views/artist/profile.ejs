<%- include('../partials/header') %>

<h1>My Artwork Visits</h1>

<p class="subtitle">
Here are the artworks you have visited and your notes.
</p>

<% if (visits.length === 0) { %>
  <div class="card">
  You haven't added notes for any artworks yet.
  </div>

<% } else { %>
  <table class="table">
    <tr>
      <th>Artwork</th>
      <th>Interested</th>
      <th>Notes</th>
    </tr>

    <% visits.forEach(v => { %>
    <tr>
      <td>
        <a href="/student/artwork/<%= v.artwork._id %>">
        <%= v.artwork.name %>
        </a>
      </td>
      <td>
        <% if (v.interested) { %>
        ⭐
        <% } else { %>
        —
        <% } %>
      </td>
      <td><%= v.notes || "No notes" %></td>
    </tr>
    <% }) %>
  </table>
<% } %>

<%- include('../partials/footer') %>