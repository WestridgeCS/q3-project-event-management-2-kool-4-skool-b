<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Campus Event Manager</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
<header class="topbar">
  <div class="logo">
    🎓 Campus Event Manager
  </div>
  <nav>
    <% if (currentArtist) { %>
      <% if (role === "artist") { %>
        <a href="/artist">Dashboard</a>
        <a href="/artist/profile">My Colleges</a>
      <% } %>

      <% if (role === "admin") { %>
        <a href="/admin">Admin</a>
      <% } %>

      <a href="/logout">Logout</a>
    <% } %>
    </nav>
</header>
<main class="container">