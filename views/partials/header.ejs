<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Super Cool Art Gallery</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
<header class="topbar">
  <div class="logo">
   Super Cool Art Gallery
  </div>
  <nav>
    <% if (currentArtist) { %>
      <% if (role === "artist") { %>
        <a href="/artist">Artist Dashboard</a>
        <a href="/artist/profile">My Work</a>
      <% } %>

      <% if (role === "admin") { %>
        <a href="/admin">Admin</a>
      <% } %>

      <a href="/logout">Logout</a>
    <% } %>
    </nav>
</header>
<main class="container">